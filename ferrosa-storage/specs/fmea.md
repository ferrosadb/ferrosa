---
crate: ferrosa-storage
doc: fmea
last_updated: 2026-09-17
---

# ferrosa-storage — FMEA / Known Issues

Failure modes ranked by **RPN = Severity × Occurrence × Detection** (1–10 each;
higher = worse). This crate is the durable substrate, so most severities are at
the top of the scale: a defect here is silent data loss or corruption.

| ID | Failure mode | Effect | S | O | D | RPN | Mitigation / status |
|----|--------------|--------|---|---|---|-----|---------------------|
| ST-1 | **Durability window under the default `Periodic` sync strategy** — writes acked before fsync are lost on power-loss/crash | Up to `sync_interval` (default 10 ms) of acked writes lost on an unclean stop; replay cannot recover what was never fsynced | 9 | 4 | 6 | 216 | **Designed window, not a bug.** `BatchSync` gives zero loss at a latency cost; `Group` bounds it. macOS uses `F_FULLFSYNC` by default (`macos-fullfsync`) because plain fsync is not a hardware flush. Document the window per deployment; choose `Batch` for zero-loss tables. |
| ST-2 | **Compaction drops/duplicates/reorders rows** vs. its inputs | Silent permanent data loss or resurrection of deleted data | 10 | 2 | 5 | 100 | Compaction validator (oracle + differential row-equivalence) gates outputs; `compaction-validator` feature exports it to the loadgen soak; `compaction_property.rs` proptest. Residual risk under UCS density edge cases. |
| ST-3 | **SSTable / S3 object corruption read back as valid data** | Wrong query results or a crash mid-scan | 10 | 2 | 5 | 100 | SHA-256 integrity metadata verified on S3 read; `write_verify` self-readback after flush; corrupt-SSTable detector + `scan_table_dir_for_corrupt` → quarantine. Residual: silent bit-rot on local NVMe between flush and first read. |
| ST-4 | **S3 write-behind backlog → local disk fills before upload drains** | Local NVMe ENOSPC; flushes stall; writes fail closed | 8 | 3 | 5 | 120 | `local_disk_free_reserve_bytes` admission check fails writes closed *before* the commit-log append; bounded upload mpsc applies backpressure; pending-upload log replays flat and generation-directory SSTable components after crash. Residual: no proactive disk-pressure-driven flush throttle beyond the reserve gate. |
| ST-5 | **Pinned (NVMe) table loses its only copy** — cache eviction or pin/unpin races delete the sole local copy with no S3 backup | Permanent data loss for a pinned table | 9 | 2 | 6 | 108 | Manifest-pinned entries are never LRU-evicted; pin→unpin enqueues S3 upload before relying on remote durability; `pin_max_bytes` evicts oldest from the pinned set only (still LRU-eligible, still local). Residual: pinned tables have no remote copy by design — node loss = data loss. |
| ST-6 | **Malformed row wedges flush or replay** (e.g. truncated timeuuid from `now()`) | Without quarantine, the flush task or node crashes in a loop | 9 | 2 | 4 | 72 | **Mitigated**: rows failing cell/clustering validation are quarantined to `quarantine/*.jsonl`; flush continues. `FLUSH_QUARANTINED_ROWS_TOTAL` non-zero is a steady-state alert (means a write bypassed the `Memtable::put` guard). |
| ST-7 | **Commit-log replay OOM** — unbounded pending mutations buffered when no schema is loaded | Node OOM on crash recovery | 8 | 2 | 5 | 80 | `max_pending_replay_mutations_without_schema` caps the legacy no-schema buffer and fails closed; normal recovery preloads bounded `schema.json`/`storage-schema.json` metadata and streams replay into registered tables. |
| ST-8 | **Reader-pool fan-in blow-up under full token overlap** (repair digests) | Memory growth → OOM under the fmem cgroup | 7 | 3 | 5 | 105 → 28 | **Mitigated:** engine-wide LRU reader pool caps resident readers (`sstable_reader_cache_cap`, default 256); range/digest walks use bounded multi-pass streaming merges; compaction holds a `CompactionGate` permit. Point reads publish maximum/high fanout and rate-limit ERROR output. Tests pin the pool to four readers under 40-file point reads and full-overlap range/digest walks. |
| ST-9 | **Manifest CAS conflict loses an SSTable from the live set** | An uploaded SSTable is not advertised → invisible data after restart | 8 | 2 | 5 | 80 | Etag-based `PutMode::Update` conditional put; conflict → reload + retry. Residual: verify retry is bounded and surfaces a loud error rather than silently dropping the entry. |
| ST-10 | **Snapshot / PITR restore applies an inconsistent point** | Restored database missing or duplicating writes around the cut | 9 | 2 | 6 | 108 | Restore manager + `restore/validation.rs` validate the snapshot + archived commit-log segments before applying; archiver ships segments to S3. Residual: end-to-end PITR coverage is still maturing (see suite-level active work). |
| ST-11 | **Lost write under memtable backpressure deadlock** | Sustained-write workload stalls or a second unbounded memtable grows | 7 | 2 | 4 | 56 | Synchronous in-line flush in `write()` on `memtable_backpressure_bytes`; `flush_guard` serializes so a runaway writer waits rather than growing a second memtable. `test_config` sets the threshold to `u64::MAX` so tiny-threshold tests do not deadlock. |
| ST-12 | **Uploaded-cache eviction deletes a not-yet-durable SSTable** — disk-pressure eviction treated manifest membership as proof of S3 durability and deleted the only local copy of a generation still queued for upload | Permanent loss of un-uploaded generations; after restart the manifest references them but they are absent in S3 *and* local disk → `skipping stale manifest entry` and divergent/partial table visibility across coordinators (#235, `agent_memory.typed_edges`) | 9 | 3 | 6 | 162 | **Mitigated**: `collect_uploaded_local_sstables` excludes any `(table_id, gen)` still listed in the pending-upload log (the entry is removed only after S3 confirms), and **fails closed** (evicts nothing) if the pending set cannot be read. Upload replay finds generation-subdirectory components. Compaction now fsyncs the output's pending-upload record before reclaiming its inputs; a marker-write failure preserves every input. Tests: `uploaded_cache_eviction_skips_generations_still_pending_upload`, `pending_upload_replay_submits_generation_directory_components`, and `compaction_keeps_inputs_when_pending_upload_log_cannot_be_written`. Residual: eviction trusts the pending log as the durability oracle rather than a direct S3 object check, so a corrupted log that omits a generation could still make it evictable. |
| ST-13 | **COUNT(*) over-count — range merger fuses overlapping SSTables into one concatenated run** — `partition_into_disjoint_runs` compared raw `smallest_key`/`largest_key` bounds as byte-comparable, but SSTables written in a Cassandra-shaped/legacy encoding store bounds that are NOT byte-comparable; mis-comparing them as token-disjoint concatenated overlapping SSTables into a single merge "run", so per-key duplicates never reached `merge::merge_partitions` | Wrong query results: `COUNT(*)` (and any metadata/row scan over the fused run) counts each row once per SSTable that holds it — per-node inflation that scales with SSTable count (observed 50807 / 27928 / 46066 for a consistent 21168-row `agent_memory.typed_edges`) | 8 | 3 | 7 | 168 | **Mitigated**: run grouping now decodes each SSTable's bounds to a `DecoratedKey` and colors intervals on decoded token order; any SSTable whose bounds fail `byte_comparable::decode` is isolated into its own singleton run (an independent heap source) so the k-way merge still groups its keys. Tests: `store::count_range_dedups_when_sstable_bounds_are_not_byte_comparable` (portable repro), `range_merger::group_by_key_isolates_undecodable_bounds_into_singleton_runs`, and the `live-infra-tests` fixture repro over the captured node SSTables. The coordinated paged-SCAN side of the same bug (rows over-DROPPED during cross-replica dedup — "Bug B") is guarded end-to-end by `ferrosa-cluster` `range_read_stream::real_typed_edges_paged_scan_delivers_every_distinct_row` (drives the real N-way merge per page over the 3-node capture; RED when the run grouping is reverted to raw bytes). Residual: the fix trusts decode-success as the disjointness gate — a decodable-but-wrong bound would still be trusted. |
| ST-14 | **Periodic S3 sync publishes an incomplete SSTable generation** — a crash-left flat generation contains only a subset of the required components | The manifest advertises an unrestorable SSTable; bootstrap later skips or fails the entry, making locally visible data absent after restore | 9 | 2 | 6 | 108 | **Mitigated**: `sync_sstables_to_s3` requires all four required components (`Data.db`, `Partitions.db`, `Rows.db`, and `Filter.db`) to be present in the collected upload set before submitting any component or publishing the manifest entry. The check is intentionally presence-only because a zero-byte `Rows.db` is valid. Tests: `engine::tests::sync_s3_rejects_generation_missing_required_rows_component`, `engine::tests::sync_s3_rejects_generation_missing_required_filter_component`, and `engine::tests::sync_s3_accepts_present_zero_byte_rows_component`. |
| ST-15 | **Concurrent schema replays replace a live table store** — two registrations observe an absent table, then the later builder overwrites the first store after it has accepted writes | Read-after-write and secondary-index queries return empty results until a later write or flush rebuilds state | 8 | 3 | 6 | 144 | **Mitigated**: table installation is compare-and-install under the table-map write lock. A losing replay merges its index declarations into the winning store and never replaces active memtable/index postings. Guarded by `engine::tests::late_table_registration_cannot_replace_live_memtable`. |
| ST-16 | **`ALTER TABLE ADD` shifts storage and index cell ordinals** — regular columns are ordered by the column-name comparator, so adding a column that sorts before an existing column re-numbers cell ordinals while existing rows and index declarations were written under the old layout | Post-ALTER reads could misattribute pre-ALTER row cells to the wrong column, and index declarations/backfills could extract postings from the wrong cell, serving false empty or wrong-column results (`agent_memory.entity_store` phonetic lookups after ddl/020) | 8 | 4 | 7 | 224 → 32 | **Fixed**: `TableStore::update_schema` remaps every positional index declaration (scalar `indexed_columns`, `fulltext_indexes`, `vector_index_configs`, filtered-index predicate clauses) through the old schema's column name before swapping the new schema; an unmappable ordinal fails loud (error log) instead of silently indexing the wrong cell. `StorageEngine::update_table_schema` now flushes dirty pre-ALTER memtable/replay rows under the old schema while holding the table-map write lock, then swaps schemas and discards the covered commit-log position. Reads already use each SSTable `SerializationHeader` to remap physical ordinals by column name, and index backfills now map current ordinals and filtered predicates through each SSTable header before building sidecars. Tests: `store::update_schema_remaps_index_ordinal_when_added_column_sorts_first`, `store::row_flushed_before_alter_still_reads_back_under_its_own_column`, `store::index_backfill_maps_current_ordinals_to_legacy_sstable_source_ordinals`, `engine::update_table_schema_preserves_unflushed_pre_alter_row_ordinals`, and `ferrosa-cql` `router::tests::phonetic_keyed_equality_matches_when_fulltext_shares_the_column`. |
| ST-17 | **Storage flush replaces the registry snapshot with an incompatible JSON array** — two components wrote different formats to `schema.json` | Next boot can register storage tables while the CQL registry starts empty, making intact SSTables unreachable | 10 | 3 | 8 | 240 → 10 | **Fixed**: `SchemaSnapshotStore` is the sole crash-safe publisher of discriminated `schema.json`; storage-only registrations use `storage-schema.json`. Reads and writes are capped at 64 MiB and stream without an intermediate JSON buffer. Corrupt, unknown, oversized, and legacy-array registry inputs are quarantined and returned as startup errors. File locking, verified staging, fsync, atomic rename, directory fsync, and three retained generations cover concurrent writers and crash boundaries. |
| ST-18 | **Startup repair ignores a MISSING SSTable component** — the zero-byte check quarantined an empty `Data.db`/`Partitions.db` but let an absent one fall through, on the reasoning that it would "fail in `open_sstable_from_dir`" | It does fail there, as `storage: I/O error: No such file or directory (os error 2)` naming no table, generation or path — and it fails EVERY read of that table rather than skipping the one generation. Observed on the ferrosa-memory native cluster: `agent_memory.mobile_control_cursor_state` and `knowledge_by_state` unreadable on all three nodes, the cursor allocator silently reverting to an older value and regressing ~10,600 cursors | 8 | 4 | 8 | 256 → 24 | **Fixed**: `sstable_health.rs` makes the decision a pure function over probed components — missing, zero-byte and unreadable all withhold the generation, `Rows.db` stays excluded because the writer legitimately emits it empty. Both startup log lines now name the component and the reason. Tests: `sstable_health::tests` (8 cases, including `a_missing_data_file_is_unusable` and `the_first_defect_in_order_is_the_one_reported`). Residual: the manifest entry itself is not reconciled, so the generation is re-probed and re-quarantined on every boot. |
| ST-19 | **Maintenance cadence creates one tiny SSTable per write batch and a stable backlog never drains without another flush** — age-only admission and global closed-WAL pressure flush every dirty table; compaction polling consumes results but does not schedule the next round | Tens of thousands of SSTables for hundreds of MiB make every point read consult/decompress thousands of files; CPU pins near 100% and services appear hung although row counts are stable | 9 | 5 | 7 | 315 → 27 | **Fixed (t_889b0d9a):** automatic age flush requires a 16-MiB volume floor; retained pressure uses actual closed-segment bytes and only flushes the table owning the oldest segment. Maintenance self-reschedules bounded smallest-input compactions; descriptor metadata is constant-size; executor task/result queues are fixed. Fixed workload: 64 tiny batches produced 63 SSTables before and one after. WAL-only restart, 16→1 no-write backlog drain, complete SSTable library, legacy remapping, and compaction/restart regressions pass. |
| ST-20 | **A partition-key-column index is dropped on restart while the planner keeps selecting it** — `reload_one_index` resolved regular and clustering targets only, so `idx_entity_by_tenant ON entity_store (tenant_id)` was counted as a dangling registration and not registered; the CQL schema still listed it, the planner chose it, and `read_by_index_each` answered an undeclared index with `Ok(())` | Every tenant-wide read of a `((tenant_id, session_id), ..)` table returned 0 rows and `count(*)` 0 — 101,848 entities read as an empty database (t_50c8bc7d) | 9 | 6 | 9 | 486 → 36 | **Fixed (t_50c8bc7d):** the reload takes the partition-key column names from the CQL schema and restores the index through `add_partition_key_index`, which backfills every SSTable (including ones flushed while the index was missing); `read_by_index_each` refuses an undeclared index with an error naming it, so a remaining schema/engine disagreement fails the read instead of shortening it. Tests: `a_partition_key_index_survives_restart_and_answers`, `read_by_index_unknown_index_is_refused_not_empty`, `a_tenant_read_scatter_gathers_every_nodes_tenant_index`, `a_node_without_the_tenant_index_fails_the_read_rather_than_shortening_it`, `a_tenant_index_the_engine_lost_is_refused_not_answered_empty`. Residual: the keyed (`read_by_index_in_partition`) and geo (`read_by_index_cell_ranges`) readers keep the empty answer — the keyed path has a designed partition-rescan fallback; the geo path does not. The resolution this row added now also serves a replicated `CREATE INDEX`, as `register_index_in_engine(&TableId, IndexToRegister)` — until that was shared, only the CQL router built indexes, so a cluster's non-coordinator nodes hit the refusal above on every tenant read (ferrosa-cluster CL-18). |
| ST-21 | **A global index read held its whole result** — postings were kept in arrival order, so the node walk deduplicated through a set of every row seen, the coordinator did the same across replicas, and the CQL arm collected every match into a `Vec` and paged by re-slicing it | Memory O(result) per request on every node and the coordinator; paging a tenant's 101,848 rows rebuilt the result on every page (O(result²)); an unpaged read returned every row in one response (t_50c8bc7d) | 8 | 7 | 6 | 336 → 32 | **Fixed (t_50c8bc7d):** every posting source is row-ordered (memtable sorted insert; sidecars sorted at write and at load, normalizing legacy files); `read_by_index_each_after` k-way merges the sources and dedups by adjacency; the coordinator merges nodes the same way; the CQL arm pages from a `(pk, ck)` cursor and folds builtin aggregates; an intersection is a merge-join. Tests: `a_keys_postings_are_kept_in_row_order_and_unique`, `a_keys_postings_are_visited_in_row_order_new_and_legacy`, `index_reads_merge_sources_in_row_order_and_resume_after_a_row`, `index_streams_merge_in_row_order_and_drop_replica_copies`, `a_resumed_tenant_read_returns_only_rows_after_the_cursor_from_every_node`, `an_index_read_streams_bounded_pages_and_resumes_from_a_cursor`, `an_index_intersection_pages_every_match_once_from_a_cursor`, `an_aggregate_over_an_index_read_folds_without_collecting_rows`, plus three source tripwires. Residual: ORDER BY / DISTINCT / non-builtin function projections over an index still collect; keyed, geo and full-text index reads still return collections. |
| ST-22 | **A partition-key index read point-read every row** — the index machinery is row-granular (built for regular/clustering columns, whose value varies per row) and d8ccb2f3 reused it unchanged for partition-key columns: the write path and builders posted `(pk, clustering)` per row and the read point-read each posting | A tenant-wide read of `agent_memory.entity_store` did ~101k single-row reads for 1,285 partitions, and `count(*)` paid the same; the memtable index held one posting per row for the hot tenant key (t_c5bccc65) | 6 | 8 | 7 | 336 → 24 | **Fixed (t_c5bccc65):** partition-key indexes post `(pk, [])` once per partition (write path, scheduler/backfill, memtable dedup, sidecar dedup); the walk streams a partition posting's rows in bounded chunks under the view-retry policy, reopens the cursor's partition, and skips legacy row postings of a partition already streamed. Tests: `a_partition_key_index_posts_once_per_partition`, `a_partition_key_index_read_streams_partitions_instead_of_point_reading_rows` (was 12 point reads for 12 rows), `a_partition_key_index_read_resumes_inside_a_partition`, `legacy_row_postings_beside_a_partition_posting_yield_each_row_once`, `a_tenant_read_pages_through_partitions_split_across_pages`. Residual: emitted rows are single-row items so the coordinator's row-level merge stays exact; regular-column indexes still point-read each matching row. |
| ST-23 | **Scalar index sidecars were heap-resident, and missing from the view after compaction, backfill and restore** — every SSTable's every sidecar was read whole onto the heap and held for the SSTable's life; compaction swapped its output in with no sidecars; backfilled sidecars were written but never installed; restore never fetched sidecars | Index memory O(every posting on the node) (31 MB of heap to open and walk one 64k-posting sidecar); after a compaction an index answered 0 of 20 compacted rows, after CREATE INDEX 0 of 15 pre-existing rows while the tracker called it current, and a node restored from S3 had no index over restored data — all until a restart, all silent (t_7ac6b0e3) | 9 | 7 | 8 | 504 → 36 | **Fixed (t_7ac6b0e3):** v2 sidecars are mapped and decoded in place (walk peaks < 64 KiB, independent of file size); atomic temp+rename writers; v1 converted at open with a spilling sort; flush maps what it writes; compaction merges input sidecars into the output's; the scheduler installs before marking indexed and marks a publish failure as failed; global reads reject any tracker state other than `Current`, so pending or failed backfill cannot leak a successful partial result; restore pulls index artifacts completely; late sidecars are uploaded by the sync. Tests: `opening_and_walking_a_sidecar_holds_a_bounded_heap_independent_of_its_size`, `a_keys_postings_are_visited_in_row_order_new_and_legacy`, `rewriting_a_sidecar_leaves_a_mapped_reader_intact`, `a_flushed_sidecar_is_read_through_a_memory_map`, `an_index_answers_for_rows_a_compaction_merged`, `a_backfilled_sidecar_is_served_without_a_restart`, `a_pending_index_read_fails_loud`, `download_sstables_from_s3_pulls_the_generations_index_sidecars`, `download_sstables_pulls_index_sidecars`, `a_sidecar_built_after_its_generation_was_uploaded_reaches_s3`, `mapped_sidecars_are_counted_for_the_life_of_the_mapping`. Residual: remote-built sidecars are not pulled local; the late-upload queue is in memory (a crash falls back to the boot rebuild). |
| ST-24 | **A legacy whole-value collection cell and a path-keyed append coexist in one memtable row** | Flush marks the SSTable complex, then panics because the live whole-value cell has no path; shutdown can exit 101 and replay repeats the crash. Cell insertion order can also trip the writer's sorted-column assertion. | 9 | 5 | 2 | 90 → 9 | **Fixed:** memtable merge detects mixed collection representations and expands the whole-value list/set/map blob into a collection-deletion sentinel plus metadata-preserving element cells before reconciliation. The resulting cells are sorted by `(column, path)`, including during commit-log replay. Regressions: `legacy_list_then_element_append_flushes_without_mixed_cells` and `legacy_set_and_map_then_element_updates_flush_without_mixed_cells`; malformed collection blobs fail the write with `InvalidData` instead of reaching the writer. |
| ST-24 | **Full-text search cost grew with compaction history** — the query path listed every `*-FTI-{index}.db` in the table directory, including sidecars compaction left behind for removed generations, and compaction wrote no FTI sidecar for its output, so the "transient" missing-sidecar fallback decoded and tokenized every compacted SSTable on every query | Per-replica `fts_match` took 7–13 s on a live 3-node loopback cluster (11 live SSTables, 5,122 sidecars / 519 MB for the index, 163 MB of sidecar-less compacted data), past the coordinator's 3 s Bulk-lane budget: every coordinated query failed with `Bulk lane timeout`, `failed_nodes=2 keys_received=0`, and memory search degraded to a scan. Stale sidecars also returned keys for rows as they were before a rewrite | 8 | 8 | 5 | 320 → 40 | **Fixed:** sidecars are resolved per live, unquarantined SSTable from the store view; compaction builds the output's sidecars before the swap; a query that finds a live SSTable without one builds and persists it once (single-flight, lock released, atomic rename); a failed build logs ERROR and leaves that SSTable to the fallback scan. Tests: `engine::fts_search_ignores_sidecars_of_generations_that_are_not_live`, `engine::fts_after_compaction_uses_a_sidecar_and_sees_only_current_rows`, `engine::fts_sidecar_less_live_sstable_is_tokenized_once_not_per_query`, and `ferrosa-cluster` `tests/fulltext_replica_bulk_lane_budget.rs` (real RPC servers + Bulk lane: 3 s timeout → ~0.26 s debug / ~0.08 s release). Residual: compaction and S3 eviction still leave index artifacts (`.sidecar`, `FTI-`, `VEC-`) of removed generations on disk — they are no longer read, but they consume disk (≈1 GB per node on the cluster where this was found). A compaction output's FTI sidecar is written into its generation directory before the S3 upload collects that directory, so it is uploaded with the SSTable; SSTables compacted before this fix get theirs on the first query after upgrade, which pays one full tokenization per SSTable and is not uploaded. |
| ST-25 | **CREATE VECTOR INDEX publishes an empty live index when rows predate the declaration** — dynamic HNSW/HVQ registration installed a fresh `VectorMemtableIndex` but did not copy the active or flushing memtable | The planner stops using correct brute-force ANN as soon as the schema advertises the index; indexed `ORDER BY ... ANN OF` returns zero rows until later writes or a flush/rebuild | 8 | 6 | 8 | 384 → 24 | **Fixed:** vector registration builds the new in-memory index from active and flushing partitions, preserving partition-key scope, before atomically publishing the view. Both HNSW and HVQ share this live layer. Tests: `store::tests::vector_index_created_after_writes_backfills_live_rows` and `ferrosa-cql` `router::tests::vector_index_registered_and_ann_orders_correctly`. |
| ST-26 | **Compaction input reads pollute the page cache and are latency-bound** — one small cached `pread` per compression chunk, no read-ahead | A compaction pass evicts query-hot pages (read-latency spikes) and runs at one round-trip per chunk on disaggregated storage (EBS) | 6 | 6 | 3 | 108 | **Opt-in fix** (`FERROSA_COMPACTION_DIRECT_READ=1`): private O_DIRECT / `F_NOCACHE` scan reader + bounded background read-ahead (`FERROSA_COMPACTION_READAHEAD_BYTES`). Fallback to buffered reads is WARN-logged and counted (`ferrosa_sstable_direct_read_fallbacks_total`); a prefetch-thread failure reaches the merge as an error, never a short read. Trade-off: scan readers bypass the shared reader pool (FMEA #11), bounded by `inputs × 2 windows` per task. Residual: the O_DIRECT syscall path and the page-cache-residency effect are verified only by byte-exactness on the macOS dev host (`F_NOCACHE`); a Linux A/B is outstanding. Tests: `direct_scan_compaction_output_matches_cached_compaction`, `direct_scan_inputs_bypass_the_reader_pool`, `scan::tests::*`, `direct::tests::direct_read_*`. |

## Top risks to act on

1. **ST-1 (RPN 216)** — the default `Periodic` sync strategy means acked writes
   are not durable for up to `sync_interval`. This is the single most important
   thing for an operator to understand and tune per workload; it is correct by
   design but high-impact if assumed away.
2. **ST-4 (RPN 120)** — local-disk pressure remains the resource-exhaustion
   path most likely to cause a node-level outage. ST-8 and ST-19 now have
   explicit reader/queue bounds and operator-visible fanout/backlog regressions.

## Detection assets

- Compaction validator (`compaction/validator/`, oracle + differential).
- `write_verify` post-flush self-readback; SHA-256 S3 integrity verification.
- Corrupt-SSTable detector + `quarantine_corrupt_generation` (`self_heal/`).
- Property/fuzz suites: `engine_property`, `compaction_property`,
  `commitlog_property`, `repair_fuzz` (`test-generators`/`fuzz-fileio`).
- Metrics: `FLUSH_QUARANTINED_ROWS_TOTAL`, compaction pool/running gauges, pin
  gauges, `sstable_read_errors`, `read_sstable_fanout_max`, and
  `read_sstable_high_fanout_total`.
