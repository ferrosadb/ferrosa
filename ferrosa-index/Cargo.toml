[package]
name = "ferrosa-index"
description = "Secondary index implementations for Ferrosa: B-tree, hash, and composite indexes"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
ferrosa-common = { path = "../ferrosa-common" }
tracing = "0.1"

[dev-dependencies]
tempfile = "3"
