.superpowers/
.fastembed_cache/
/target/
**/target/
/target-*/
/dist/
/releases/

# .claude/skills/ contains symlinks to the research repo (~/.claude/skills/).
# Other developers: run setup.sh from the research repo, then re-link with /warp.
.worktrees/
.claude/worktrees/
# Linked worktrees created as siblings inside the repo root. Without this they
# stage as mode-160000 gitlinks: a public clone gets broken, empty directories
# pointing at SHAs it cannot fetch.
.wt-*/
# Agent process state and runtime logs. Goal text, acceptance criteria, attempt
# counts and verifier notes -- internal, and permanent if committed here.
.forge/
logs/
.fastembed_cache/
rootfs/test_key
rootfs/test_key.pub

# Generated by quarantine integration tests
quarantine/
.DS_Store
/ferrosa-jepsen/rootfs/test_key
/ferrosa-jepsen/rootfs/test_key.pub

# Python build artifacts
__pycache__/
*.py[cod]
*$py.class
.pytest_cache/

# CI/local smoke build artifacts
tests/ferrosa.deb
tests/ferrosa
target-linux/
examples/build/
